{
  "name": "azure-hapi",
  "version": "1.0.0",
  "description": "simple application to deploy to azure",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "hapi": "^9.3.1"
  }
}
